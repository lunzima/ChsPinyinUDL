# 多来源词频词典融合方法

这份文档写的是一套把若干个互相独立的"词条—读音—频率"列表，合并成一份频率
量纲统一、冗余和异常都处理过的单一词典的方法。跟具体来源是谁、合并出来的
东西最终拿去干什么都没关系——只要手上的数据能整理成下面约定的格式，这套
方法就能套用。全篇只写思路和伪代码，细节（异常处理、边界条件、具体库的
调用方式）留给实现的人自己填。

## 问题出在哪

几份独立维护的词频表放在一起，麻烦通常出在三个地方。

第一是量纲对不上。各家统计口径、语料规模、平滑方式都不一样，同一个词在
一份表里频率是两百，在另一份表里可能是二十万，直接拿数字相加或者求平均
没有意义，得先想办法把它们换算到同一把尺子上。

第二，也是最容易被忽略的一点：某一份来源里偶尔会混进局部异常值。可能是
语料本身被污染了（比如某种固定格式的模板文本被当成正文统计进去），也可能
是提取环节哪里算错了，反正结果就是某个词条的频率高得不正常。如果拿这种
带病的数据去校准别的来源，异常会顺着校准这条链路扩散出去，越滚越大。这是
整套方法里花心思最多的地方。

第三，只有一份来源收录、别的地方都没有的词条，可信度天然要打折扣，不能
跟被好几份独立来源共同验证过的词条一视同仁。

后面的流程就是按顺序把这三件事一个个解决掉，每一步都能单独拿出来验证、
单独调参数，不需要牵一发动全身。

## 输入长什么样

每份来源是一个文本文件，正文部分每行一条记录，大致是这个样子：

```
词条1  拼音1 拼音2 ...  频率
词条2  拼音1           频率
```

多音节词的拼音之间用空格隔开，每个音节只允许小写字母和数字，不能混进别的
字符——这是为了兜住有些来源在碰到生僻字转写失败时，直接把原字符吐回来
当"拼音"用的脏数据。频率这一列可以没有，没有就当1处理；出现负数或零也
按1处理，因为后面要取对数，不能留着非正数。

文件头部允许有一段可选的说明性前缀，解析的时候直接跳过就行。

## 第一步，单份来源自己的清洗

逐行读，拼音格式不对的行直接扔掉。同一个(词条, 拼音)组合出现了不止一次，
取这些重复记录的平均值，而不是取最大值或者用最后一条覆盖前面的——用
平均值是特意的：万一其中一条是异常的极高值，平均能把它的影响摊薄，取最大
值则正好相反，等于主动放大了异常值的话语权。

```
函数 清洗单份来源(文件路径):
    逐行读取正文部分
    跳过空行、注释行、拼音格式不合法的行
    按 (词条, 拼音) 分组，频率取组内平均值
    返回 {(词条, 拼音): 平均频率}
```

## 第二步，同一份来源内部的复合词去冗余

一份来源内部，长词条往往就是短词条硬拼起来的，而且词越长，出现次数越少，
频率统计的噪声也越大，可信度天然不如它拆开来的那几个部分。如果一个长词
能被词典里已经收录的、频率明显高得多的几个子词精确拼出来，这条长词记录
本身的价值就很可疑，该拿掉。

这里有几个容易踩坑的地方，得specific说一下：

拆分时不能只比对拼音，得连汉字一起比对。只按拼音音节切分会出现同音不同字
的误伤——拼音路径拼得通，但拼出来的汉字跟原词不是一回事，那就不能算冗余。

切分方式用正向最长匹配，优先切出最长的、能在词典里找到的子串，避免把词
过度打碎成一堆单字。

切分出来的段数不设上限——一个长词如果真能被好几个已收录、频率高出一大截
的子词精确拼出来，段数越多反而是越强的冗余信号，没理由卡一个人为的上限。
真正该把关的是频率上的双重门槛：长词本身要足够冷门，而且拆出来的子词里
最不常见的那个，频率也得比长词高出一大截，两个条件都满足才判定为冗余，
不需要靠限制段数这种间接的手段来防误伤。

```
函数 找冗余复合词(词条列表):
    建索引: (词条, 拼音) -> 平均频率
    冗余集合 = 空
    对词条列表中每条 (词, 拼音, 频率):
        若 频率 > 绝对频率上限 或 拼音音节数 < 2 或 字数≠音节数: 跳过
        用正向最长匹配尝试把 (词, 拼音) 切成词典里已有的子词序列
        若切不完整、或段数超出合理范围: 跳过
        取子词序列里最低的那个频率
        若 最低子词频率 >= 频率 × 放大倍数门槛:
            该词条判定为冗余，加入冗余集合
    返回 冗余集合
```

## 第三步，离群条目检测

这一步是整套方法里最要紧的防污染设计，起因是一次真实的排查：某个三字以上
的长词条，在某一份来源里的频率排到了全库前0.1%以内，可其余所有同样收录
了这个词的来源里，它都只是个再普通不过的中低频词。逐一核实之后能确认，
所有来源真正的顶层——前0.1%的高频区——清一色是一两个字的虚词、代词、
常用动词和介词，三个字以上的词条正常情况下根本挤不进这个区间。某份来源
里出现了，基本可以断定是语料污染或者提取过程中的数值错误，不是这个词真的
被用得那么频繁。

检测规则定得很朴素，也很保守，只抓确定性很高的那批，不追求一个不漏：词长
达到三个字、并且在自己所在来源内部的排名百分位达到99.9%，就判定为这个
(来源, 词条)组合的离群点。

标记出来之后不是把这一条数据删掉就完事，而是在接下来构建参考尺度、拟合
校准曲线、加权平均合并这三个环节里都统一把它排除在外——目的是防止某份
来源的个别异常值，顺着校准这条链路传染到所有跟它有交集的其他来源，这才是
真正要防住的事。

```
函数 找离群点(全部来源):
    离群集合 = 空
    对每份来源:
        算出该来源内部每个词条的排名百分位（越高频排名百分位越接近100）
        对每个词条: 若 字数>=3 且 排名百分位>=99.9%: 加入离群集合
    返回 离群集合
```

## 第四步，构建一个不偏向任何单一来源的共识尺度

挑一份数据量最大、动态范围最广的来源当基准，其他都往它上面校准，是最
容易想到的做法，但风险也最大：基准来源自己的个别异常值（就是上一步要抓
的那种）会顺着校准曲线污染到所有跟它有重叠的来源，相当于把风险最大的
那份来源架到了权力最大的位置上。

更稳妥的做法是不让任何一份来源单独说了算，先攒出一个共识：每份来源各自算
出内部排名百分位（离群点已经排除），同一个词条只要有两份及以上独立来源
同时给出了数据，就取这些百分位的中位数，当作这个词条的共识排名位置。只有
一份来源收录的词条谈不上共识，这一步先放着，交给后面的孤立词规则处理。

有了共识排名位置，还差一步——把它换算回一个具体的频率数值。这里选一份
来源的数值分布当模板，用共识排名位置去查这份分布对应哪个具体数值，但这份
模板来源只负责提供"数值大概是什么形状、动态范围有多宽"，不再对任何一个
具体词条的个别取值有特权。挑模板的原则是优先选动态范围相对紧凑、并且已经
独立核实过数据质量的来源：动态范围越宽的来源越不适合当模板，一旦有漏网的
异常值没被上一步的检测抓到，经这份模板一放大，能放大到多离谱是没有上限的；
动态范围紧凑的来源天然把这种潜在风险摁在了一个有限的范围里。

```
函数 构建共识尺度(全部来源, 离群集合, 模板来源):
    对每份来源算排名百分位（跳过离群点）
    对每个词条: 收集支持它的各份来源给出的百分位
    共识 = 空
    对每个至少有两份来源支持的词条:
        共识排名位置 = 这些百分位的中位数
        换算 = 在"模板来源"的数值分布里，找共识排名位置对应的那个具体数值
        共识[词条] = 换算结果
    返回 共识
```

## 第五步，每份来源分别向共识尺度校准

有了共识尺度之后，所有来源——包括提供数值模板的那一份自己——都要对着这个
共识做一次校准，拟合方式用保序回归（isotonic regression），在对数空间里做。
选保序回归是因为它只要求"原始值越大、校准后的值不会变小"这一条单调性约束，
不强行套线性或者对数这种具体的函数形式，比较适合频率这种分布形状本来就
千奇百怪的数据。

拟合完了怎么评价这份来源校准得好不好，用斯皮尔曼秩相关系数的平方衡量，
这个数字直接拿去当下一步加权平均时这份来源的权重——秩相关性差，说明这份
来源内部的相对排序跟大家的共识对不上，加权合并的时候自然该少听它的，而不
是不管质量好坏都算一票。

```
函数 拟合校准(某份来源, 共识尺度, 离群集合):
    收集这份来源里、跟共识尺度重叠、且没被标记为离群点的 (原始值, 共识值) 对
    若重叠样本太少: 返回 空模型（这份来源退化为只用于判断词条是否出现过）
    在对数空间对这些点做保序回归拟合，模型记作 f
    算这份来源原始排序跟共识排序的斯皮尔曼相关系数 rho
    返回 (f, rho)
```

## 第六步，加权合并，滤掉孤立词

有了每份来源自己的校准函数和可信度（rho的平方），就可以把它们的校准值
按可信度加权平均了，离群点照样排除在外——一份来源被标记为某个词条的离群
点，只影响这份来源在这个词条上的数值要不要采信，不影响这个词条本身还留
不留在最终结果里，那是另一套规则管的事。

孤立词的判定对所有来源一视同仁，不给任何一份来源开后门。一个词条只被
极少数来源收录，可信度天然存疑，具体要几份来源支持才算数，按词长分层来
定：词越长，越容易是某份来源自己的用词习惯或者噪声，需要更多份独立来源
交叉印证才站得住脚，门槛应该比短词更严一些。

```
函数 加权合并(全部来源, 各来源校准值, 各来源可信度, 离群集合):
    对每个词条统计覆盖了它的来源数
    对每个词条: 按 可信度的平方 加权，汇总各来源校准值的加权平均（跳过离群点）
    对每个词条: 若覆盖来源数够不上门槛（长词门槛更高）: 从结果里剔除
    返回 合并结果
```

## 可选步骤：用语言模型再筛一遍

如果最终场景比较在意词条得是个语义上完整的"整词"，而不是被硬切碎的片段，
可以额外接一个开源的小型语言模型分词器，做一轮补充检查：把词条丢进去分词，
如果切出来的token数比字数还多，说明这个词在模型眼里根本不是一个自然的
整体，直接扔掉；token数没超过字数的，按 token数的倒数 调整一下权重，切得
越碎权重打折越狠。这一步不是必须的，看不看重"整词感"这个诉求。

## 可选步骤：手工把某个词条的权重锚定到另一个词条上

有些词条是出于统计流程管不到的理由手动加进去的，希望它的权重落在"跟某个
真实存在的参照词条差不多"这样一个可控、不显眼的位置，而不是套统计流程
自然算出来的权重。比起定位到"分布前百分之多少"这种抽象说法，直接把权重
设成某个具体参照词条当下的权重更直观，也更容易控制显眼程度。参照词条万一
因为某些原因没能留在最终结果里，退回到一个固定的分位点兜底就行。

## 最后一步，把权重压缩进一个统一区间再输出

所有保留下来的校准值，最后线性映射进一个固定的整数区间——比如16位无符号
整数的上限65535——再落盘。选一个有实际依据的边界（而不是随手定个一百万
这种数字）还带来一个额外的好处：几十万条互不相同的校准浮点值压进一个小
得多的整数区间后，原本在低端彼此只差一点点的一大批数值会自然折叠到同一批
小整数上，包括相当数量并列在最低值的情况，不用额外写聚类逻辑，纯粹是压缩
目标区间之后的数学必然结果。

排序上，按权重从高到低排，权重相同时再按词条文本、然后按读音排，这样
每次重新生成的文件物理行序都是确定、可复现的，不会因为字典遍历顺序之类
的实现细节产生随机抖动。

## 串起来看整体流程

1. 逐份来源清洗、去掉内部的冗余复合词
2. 跨份检测离群条目
3. 构建不依赖单一来源的共识尺度
4. 每份来源分别向共识尺度做保序回归校准
5. 按可信度加权合并，滤掉孤立词
6.（可选）用语言模型分词器再筛一遍整词性
7.（可选）手工锚定个别词条的权重
8. 压缩权重区间，排序，落盘

## 大致的数量级感受（不是硬指标）

拿一次真实跑过的、十份来源、单份规模从几万到近两百万条不等的合并做参照，
各阶段大致是这么个量级，实际数字会随输入数据的特点浮动很大，这里只是给
一个直觉：

拼音格式清洗剔除的比例普遍很低，大部分来源是零剔除，个别来源在1%~2%左右。

同一份来源内部判定为冗余复合词的比例波动非常大，从几乎为零到六成以上都
出现过——跟这份来源本身收录词条的构成方式关系很大，词条本来就偏"整词、
少堆砌"的来源冗余率低，习惯把大量短语组合都单独收一条的来源冗余率能很高。

离群点占全部(来源,词条)组合的比例很小，在千分之一量级以内。

能构成共识（两份及以上独立来源支持）的词条，大概占全部词条的四成出头，
剩下的要么是被离群点检测排除了，要么就只有一份来源收录，交给孤立词规则
处理。

校准的可信度（rho）分化得比较明显：数据质量好的来源普遍能到0.7以上，
个别质量很差的来源可能低到0.1都不到，加权合并时基本说不上话。

孤立词规则滤掉的比例可能相当高，六成以上的候选词条只被很少数来源收录，
够不上交叉印证的门槛，说明"多来源共同验证"这件事本身就是个很强的筛子。

语言模型再筛一遍剔除的比例不高，个位数百分比。

## 几个明知道没做全的地方

字数和拼音音节数对不上的词条（比如涉及儿化音、多音字的一些边界情况），
复合词去冗余这一步直接跳过不处理，保守起见，宁可漏判也不强行拆。

离群点检测的门槛（词长三字起、百分位99.9起）刻意定得很紧，只抓确定性
很高的那批，不追求一个不漏，为的是不去误伤真正高频的长词。

数值模板来源怎么选，本身是个需要人工判断、而且理应提前独立核实过数据
质量的决定，这一环没法自动化，也不该自动化。
